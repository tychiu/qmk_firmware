/* Uncomment to define the combo time limit. Defaults to 100ms if not defined */
/* ja:　コメント解除することで、同時打鍵判定の制限時間を定義できる。定義しない場合の初期値は100ミリ秒 */
// #define COMBO_TERM 100

/* Uncomment to define the minimum time difference between combos (overlap duration). Defaults to 2 x DEBOUNCE if not defined */
/* ja: コメント解除することで、同時打鍵の最小間隔（重なり厚み）を定義できる。定義しない場合の初期値は 2 × DEBOUNCE */
// #define OVERLAP_TERM (2 * DEBOUNCE)
